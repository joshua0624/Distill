# Feed filtering thresholds
# Items with a relevance_score below min_relevance_score are hidden from the feed.
# Unscored items (NULL) always appear until the batch job runs.
min_relevance_score: 50

# Phase 3: Controlled Serendipity
# Hard daily cap on discovery items surfaced per day. Cannot be exceeded.
discovery_daily_cap: 5
